{#
/**
 * @file
 * Default theme implementation to display a node.
 *
*/
#}

<article>
  {% for item in items %}
    <div class="student-wrapper">
      <div class="student-img"><img src="{{ item.url }}"></div>
      <div class="student-text">{{ item.title[0].value }}</div>
    </div>
  {% endfor %}
</article>
